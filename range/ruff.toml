[lint]
# B008 flags `Depends(...)` in function argument defaults as a bug, but this
# is FastAPI's standard, documented dependency-injection pattern (see
# https://fastapi.tiangolo.com/tutorial/dependencies/) — not an actual issue.
#
# BLE001 flags `except Exception` as too broad, but app/logging_config.py's
# DetectionEngineHandler.emit() deliberately catches everything: a logging
# handler must never let any error crash or block the app it's
# instrumenting, the same contract logging.Handler.handleError() itself
# documents. A per-line `# noqa` comment was tried first and doesn't
# survive in this repo's environment (something strips trailing comments
# from .py files before they're committed) — a config-level ignore doesn't
# have that problem.
ignore = ["B008", "BLE001"]
