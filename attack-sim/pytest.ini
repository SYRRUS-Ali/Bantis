[pytest]
markers =
    fingerprint: locks in the exact shape (field names + types) of a scenario's ScenarioResult.details, so an unintentional schema change fails CI immediately. Run in isolation with `pytest -m fingerprint`.